<Page x:Class="BookBuddy.Views.StartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BookBuddy" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="StartPage"
      DataContext="{Binding Source={x:Static local:App.Current}, Path=Resources.mvm}">


    <Grid>
        <TextBox Text="Just to see if works."></TextBox>
        <TextBox HorizontalAlignment="Left" Margin="221,219,0,0" TextWrapping="Wrap" Text="{Binding currentUser}" VerticalAlignment="Top" Width="433" Height="81"/>

    </Grid>
</Page>
